var app = getApp()
Page({

  /**
   * 页面的初始数据
   */
  data: {
    
  },

  /**
   * 生命周期函数--监听页面加载
   */
  onLoad: function (options) {
    this.setData({
      rechargeAmount: app.globalData.rechargeAmount
    })
    //调用loading，等待2s后跳转余额界面
    wx.showLoading({
      title: '支付中',
    })
    setTimeout(function () {
      wx.hideLoading();
      app.globalData.balance += app.globalData.rechargeAmount;
      app.globalData.balance = parseInt(app.globalData.balance);
      app.globalData.balance
      wx.redirectTo({
        url: '/pages/balance/balance'
      })
    }, 2000)
    
  },

  /**
   * 生命周期函数--监听页面初次渲染完成
   */
  onReady: function () {
    
  },

  /**
   * 生命周期函数--监听页面显示
   */
  onShow: function () {
    
  },

  /**
   * 生命周期函数--监听页面隐藏
   */
  onHide: function () {
    
  },

  /**
   * 生命周期函数--监听页面卸载
   */
  onUnload: function () {
    
  },

  /**
   * 页面相关事件处理函数--监听用户下拉动作
   */
  onPullDownRefresh: function () {
    
  },

  /**
   * 页面上拉触底事件的处理函数
   */
  onReachBottom: function () {
    
  },

  /**
   * 用户点击右上角分享
   */
  onShareAppMessage: function () {
    
  }
})